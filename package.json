{
  "name": "scamshield-alexa",
  "version": "1.0.0",
  "description": "ScamShield for Alexa+: a voice-first scam-checking MCP server (Streamable HTTP)",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --env-file-if-exists=.env server.js",
    "test": "node --test --test-concurrency=1 test/*.test.js",
    "bench": "node scripts/bench.js"
  },
  "engines": {
    "node": ">=20.12"
  },
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.1",
    "express": "^5.2.1",
    "zod": "^4.6.5"
  }
}
